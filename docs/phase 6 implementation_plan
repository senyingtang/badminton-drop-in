# Phase 6：公開報名頁面與球員回報

## 背景
Phase 6 旨在降低團主的通訊成本。透過專屬的**公開報名連結**，球員可以自行查看場次資訊並加入名單；同時開放**球員端回報比分**，讓團主只需審核，不需手動輸入每一場賽果。

---

## User Review Required

> [!IMPORTANT]
> **報名隱私**：公開頁面會顯示已報名球員的名稱。若有隱私疑慮，可設定「僅顯示部分名稱」或「僅登入後可見名單」。Phase 6 預設採「公開顯示名稱」以利球員核對搭檔。
>
> **比分回報權限**：球員僅能回報自己有參與的 Matches。若多人回報且分數不一致，系統會標註「衝突」，需由團主進行最終核定。

---

## Proposed Changes

### 1. 公開報名頁面 (`/s/[code]`)

#### [NEW] `/app/(public)/s/[code]/page.tsx`
- **輕量化介面**：不強制登入即可查看場次標題、時間、地點、費用、當前報名進度（正選/候補人數）。
- **報名動作**：
  - 未登入：導向登入頁，登入後自動跳回報名。
  - 已登入：顯示「我要報名」按鈕。
- **自動分類**：依據 `session.max_participants` 自動決定進入「正選」或「候補」。

---

### 2. 球員端回報介面 (Player Portal)

#### [NEW] `/app/(protected)/my-matches/page.tsx`
- 顯示當前進行中（Locked）且玩家參與的 Matches。
- 提供直覺的「報名比分」按鈕，前端邏輯與 Phase 4 的團主輸入類似，但限制對象。
- 寫入 `match_score_submissions` 表。

#### [MODIFY] `MatchCard.tsx`
- 偵測到有球員回報時，顯示「球員已提交」狀態。
- 團主可點擊查看建議分數並「一鍵採納」。

---

### 3. 場館管理深化

#### [MODIFY] `VenueDetail` & `VenueForm`
- 新增場館聯絡電話、詳細地址、Google Maps 整合連結。
- **場館時段模版 (`venue_time_slots`)**：
    - 實作時段管理器，定義常用的「週五 19:00-22:00」等規則。
    - 建立 Session 時，可從模版一鍵載入時間與場地數。

---

### 4. 通知系統 (Internal Notifications)

#### [NEW] `NotificationBell` & `NotificationProvider`
- 監聽 `notifications` 表的即時變動。
- 當發生以下事件時推送通知：
    - 候補成功遞補為正選。
    - 場次報名成功。
    - 比賽分組已產生。
    - 被團主標記警示或移除名單。

---

## 預期成果 (Verification Plan)

### Automated Tests
- [ ] 驗證 `/s/[code]` 路由對於匿名使用者的可存取性。
- [ ] 模擬兩位球員回報衝突比分，確認 `match_score_submissions` 紀錄正確。

### Manual Verification
1. 團主產生場次報名連結，於無痕視窗開啟，確認資訊正確顯示。
2. 使用球員帳號點擊連結報名，確認自動歸入正選或候補。
3. 比賽鎖定後，使用球員帳號進入「我的比賽」回報比分，確認團主端能看到回報紀錄。
4. 編輯場館地址並點擊 Google Maps 連結確認導航正確。
