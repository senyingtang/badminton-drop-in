# Phase 7：通知中心與管理後台

## 背景
Phase 7 是平台的管理與維運終點。除了強化使用者的**即時通知體驗**外，我們將為平臺管理員 (`platform_admin`) 打造**管理後台**，使其能監控全站數據、處理異常帳務、並匯出重要營運報表。

---

## User Review Required

> [!IMPORTANT]
> **管理權限**：後台頁面僅具備 `platform_admin` 角色者可進入。我們需要確保 RLS 安全性，防止團主或場主存取全站數據。
>
> **匯出格式**：初步支援 CSV 格式匯出。若有 PDF 報表需求，可於後續擴充。

---

## Proposed Changes

### 1. 平台後台 (Admin Dashboard)

#### [NEW] `/app/(admin)/dashboard/page.tsx`
- **關鍵指標 (KPIs)**：
  - 本月總場次、活躍團主數、全站總收入 (Usage Charges)。
  - 伺服器健康度與資料庫連線狀態摘要。
- **異常監控**：
  - 偵測長時間未結算的 Session。
  - 餘額異常（負數過大）的錢包清單。

#### [NEW] `/app/(admin)/users/page.tsx`
- 全站使用者管理，支援查詢、手動停權、或調整會員等級。

---

### 2. 營運支援工具 (Support Tools)

#### [NEW] 錢包手動調整介面
- 管理員可手動增加/扣除使用者餘額（需填寫原因，寫入 `kb_wallet_transactions` 並記錄 Audit Log）。

#### [NEW] 評價內容審核
- 處理被檢舉的球員評價，管理員可設定 `is_hidden` 以隱藏爭議內容。

---

### 3. 通知中心強化

#### [MODIFY] 全站佈局整合通知鈴鐺
- 點擊開啟側面通知抽屜。
- 支援「全部標記為已讀」與「一鍵清空」。
- **即時觸發器**：
  - 新增 Database Triggers，當 `session_participants` 狀態變更時，自動寫入 `notifications`。

---

### 4. 數據匯出功能

#### [NEW] `lib/utils/export.ts`
- 實作 CSV 產生邏輯。
- 場次詳情頁：匯出參與者清單與級數分佈。
- 帳務頁面：匯出年度/月度收支明細。

---

### 5. 操作稽核 (Audit Logs)

#### [NEW] `/app/(admin)/audit/page.tsx`
- 展現全站的變更歷史。
- 支援篩選特定使用者執行過的「敏感操作」（如修改級數、刪除場次、調整錢包）。

---

## 預期成果 (Verification Plan)

### Automated Tests
- [ ] 驗證非管理員身分（如單純球員或團主）無法進入 `/app/(admin)`。
- [ ] 模擬通知觸發流程，確認 Realtime 推送至前端。

### Manual Verification
1. 使用管理員帳號登入，查看全站即時營運指標。
2. 進行一次手動錢包加點，確認帳務記錄中顯示為「管理員調整」。
3. 在場次中觸發遞補，確認球員端會立刻收到通知鈴鐺紅點。
4. 嘗試匯出場次 CSV，確認中文字元與級數資料顯示正常。
